{
  "engineer-safety-hooks": {
    "PreToolUse": [
      {
        "matcher": "run_command",
        "description": "Advisory screen for destructive filesystem or git operations (fail-open; blocks only where a runtime registers this PreToolUse denial)",
        "command": "engineer/hooks/guard.sh",
        "enforcement": "advisory",
        "guard_patterns": [
          "rm -rf /",
          "rm -rf ~",
          "git push.*--force",
          "DROP DATABASE"
        ]
      }
    ]
  },
  "checklists (not runtime events; owned by skill protocol)": {
    "PreCommit": [
      {
        "description": "verify phase runs blast-radius check before commit (gitnexus when available, else git diff)",
        "optional": true,
        "fallback": "git status --short && git diff --stat"
      }
    ],
    "SessionEnd": [
      {
        "description": "ship phase writes HANDOFF.md or mem_session_summary when Engram is available",
        "optional": true
      }
    ]
  }
}
