{
  "engineer-safety-hooks": {
    "PreToolUse": [
      {
        "matcher": "run_command",
        "description": "Guard against destructive filesystem or git operations (enforced by guard.sh; patterns are fallback)",
        "command": "engineer/hooks/guard.sh",
        "guard_patterns": [
          "rm -rf /",
          "rm -rf ~",
          "git push.*--force",
          "DROP DATABASE"
        ]
      }
    ],
    "PreCommit": [
      {
        "description": "Run GitNexus detect-changes before committing when available (optional, degraded mode OK)",
        "command": "gitnexus detect-changes --scope all --repo .",
        "optional": true,
        "fallback": "git status --short && git diff --stat"
      }
    ],
    "SessionEnd": [
      {
        "description": "Engram session summary check when available (optional)",
        "action": "mem_session_summary",
        "optional": true
      }
    ]
  }
}
