{
  "scientist-safety-hooks": {
    "PreToolUse": [
      {
        "matcher": "run_command",
        "description": "Advisory screen for destructive SQL on production/warehouse tables or data loss (fail-open; blocks only where a runtime registers this PreToolUse denial)",
        "command": "scientist/hooks/guard.sh",
        "enforcement": "advisory",
        "guard_patterns": [
          "DROP TABLE.*(prod|production)",
          "DROP DATABASE",
          "TRUNCATE.*(prod|production)",
          "rm -rf /data",
          "aws s3 rm.*--recursive.*prod"
        ]
      }
    ]
  },
  "checklists (not runtime events; owned by skill protocol)": {
    "PrePromotion": [
      {
        "description": "Verify bundle manifest and HMAC signature before production promotion",
        "required_files": [
          "manifest.json",
          "manifest.json.sig"
        ]
      }
    ],
    "SessionEnd": [
      {
        "description": "promote-gate writes receipt / HANDOFF.md entry (mem_save when Engram is available)",
        "optional": true
      }
    ]
  }
}
